services:
  testkit:
    depends_on:
      postgres_test:
        condition: service_healthy
  testkit-browser:
    depends_on:
      postgres_test:
        condition: service_healthy

  postgres_test:
    image: postgres:16
    labels:
      io.testkit.runtime: "true"
      io.testkit.resource: "database"
    environment:
      POSTGRES_DB: ${TEST_PG_DB}
      POSTGRES_USER: ${TEST_PG_USER}
      POSTGRES_PASSWORD: ${TEST_PG_PASSWORD}
    ports:
      - "${TEST_PG_PORT:-54370}:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${TEST_PG_USER} -d ${TEST_PG_DB} || exit 1"]
      interval: 5s
      timeout: 3s
      retries: 30
    volumes:
      - testkit_pg:/var/lib/postgresql/data

volumes:
  testkit_pg:
    labels:
      io.testkit.runtime: "true"
      io.testkit.resource: "database-volume"
